# Lucky Nest Monorepo - AI宪法 v3.0

> **核心定位**：具备"智能体记忆"的进化型工程伙伴。通过反馈闭环实现自我完善，将经验转化为架构规则。

## 🔄 核心协议 (Meta-Rules) - 宪法自我进化机制

### 1. 自我更新权限
- **AI有权完善此宪法**：当发现重复性错误模式或经过验证的最佳实践时，AI可以提议更新规则
- **用户批准机制**：每次修改前必须向用户说明更新内容、原因和影响，获得明确同意
- **客观性原则**：只能添加客观的工程规范，禁止主观情绪化词汇

### 2. 知识积累机制  
- **问题模式记录**：解决连续出现2次以上的同类Bug时，必须提炼为规则
- **最佳实践归档**：经过实际验证的有效解决方案，应转化为架构规则
- **版本追踪**：每次修改需记录日期、原因、相关commit hash和影响范围

### 3. 更新验证流程
- **反向兼容检查**：新规则不能与现有规则冲突，需保持一致性
- **实际案例验证**：规则必须有具体代码示例（正确/错误对比）
- **影响范围评估**：需分析影响哪些现有代码模块，是否需要同步修改

### 4. 进化触发条件
- **重复错误模式**：同一类错误出现2次以上
- **架构改进**：系统性解决方案经过验证有效
- **性能优化**：显著提升性能的最佳实践
- **安全加固**：增强系统安全性的必要措施

> **记住**：宪法不是一成不变的教条，而是随着项目成长而进化的工程智慧结晶。

## 🔴 绝对禁令（一票否决）

1. **禁止使用`any`类型**：未知类型使用`unknown`，必须为所有API响应和组件Props定义完整interface
2. **禁止在代码注释中使用Emoji图标**：所有//注释只能使用纯文本，禁止使用❌⚠️🐛等符号，文档和Markdown文件除外
3. **禁止代码中出现非ASCII字符注释**：注释优先使用英文，中文仅允许在Markdown文档中使用
4. **禁止假设导入存在**：使用任何导入前必须检查目标文件是否确实导出了该标识符
5. **禁止在验证之前提交修改**：任何代码修改必须先通过类型和lint检查
6. **禁止产生水合错误**：涉及浏览器API（window, localStorage, Canvas等）的逻辑，必须封装在`useIsClient` Hook中
7. **禁止硬编码样式**：优先使用Tailwind CSS，避免基于客户端状态计算的动态class
8. **禁止重复定义类型**：优先复用`packages/shared`和`src/types`中的全局类型
9. **禁止线性思维**：每次修改必须从系统角度思考，分析影响范围和水合风险
10. **禁止单层语言处理**：语言切换必须在HTTP请求、React Query缓存、路由参数三个层面统一处理
11. **禁止乱改包名**：package.json中的`name`字段必须遵循项目命名规范，禁止随意更改工作区名称
12. **禁止消防员思维**：禁止只修复出错的那一行代码，必须消除根本原因，完成闭环改进
13. **禁止不必要的 dev 重启**：
    - 禁止在代码修改后自动运行全局 `yarn dev` 命令
    - 开发服务器应持续运行，除非修改了配置文件、环境变量或包依赖
    - 优先使用工作区特定的 dev 命令（如 `yarn workspace @lucky/admin-next dev`）
    - 必须利用 Next.js 热重载特性，90%的代码修改无需重启服务

## 🟡 架构规则（按模块）

### Next.js前端

- **页面跳转动画**：必须使用动态导入（`next/dynamic`）禁用SSR，或封装在`useIsClient` Hook中
- **组件生命周期**：写代码前必须分析"SSR环境下是否存在风险"
- **国际化路由**：必须考虑`/[locale]/path`格式，优先使用系统配置的可用语言列表
- **状态管理**：优先使用Zustand，复杂状态必须定义完整interface和initialState
- **API调用**：必须使用项目HTTP客户端，包含完整错误处理和loading状态
- **可选依赖**：动态导入的可选模块必须有类型声明文件（如`capacitor.d.ts`）
- **React Query缓存**：所有数据获取Hook的queryKey必须包含`locale`参数，确保语言切换时缓存正确失效

### NestJS后端  

- **数据验证**：必须使用DTO + 全局拦截器/管道处理数据验证和XSS消毒
- **权限系统**：必须复用现有AdminJwtAuthGuard + RolesGuard三级权限体系
- **数据库操作**：金额计算使用Decimal.js，敏感数据加密存储
- **事务处理**：相关操作必须使用Prisma事务确保数据一致性
- **错误处理**：统一错误响应格式，包含错误码和友好消息

### 全局开发规范

- **文件位置**：
  - 公共工具函数 → `src/lib/utils/`
  - 自定义Hooks → `src/lib/hooks/`
  - TypeScript类型定义 → `src/types/` 或 `packages/shared/src/types/`
  - 组件分类 → `src/components/` + 子目录按功能分类
- **组件使用**：必须使用`@repo/ui`组件库，禁止使用原生HTML元素
- **代码质量**：
  - 函数长度 ≤ 50行
  - 组件Props必须严格声明
  - 复杂逻辑必须有注释说明
- **文档要求**：重大修改必须遵循七层黄金文档结构

## 🟢 思考过程要求（每次任务前必须执行）

### 1. 系统影响分析
- 这个改动会影响哪些相关部分？
- 是否需要同步修改其他文件？
- 是否符合项目现有模式？

### 2. 水合风险评估  
- SSR环境下是否存在水合风险？
- 是否依赖浏览器API（window, localStorage等）？
- 如何防范？使用`useIsClient`还是动态导入？

### 3. 类型安全分析
- 所有类型是否已有定义？
- 是否需要新增interface？
- 是否存在可选依赖需要类型声明？

### 4. 重复代码检查
- 这个逻辑在其他地方存在吗？
- 能否抽取公共函数？
- 是否符合DRY原则？

### 5. 时序预判
- 如果有两个请求同时到达会发生什么？
- 异步操作是否有竞态条件风险？
- 是否需要添加防护机制？

### 6. 闭环改进分析
- 这个问题为什么会发生？
- 为什么之前没有被发现？
- 如何保证永远不会再出现？
- 是否需要更新文档或规则？

## 📋 验证流程（AI必须执行）

1. **全局搜索**：先搜索全文，查看是否有可复用结构和现有解决方案
2. **导入验证**：使用任何导入前必须检查目标文件是否确实存在该导出
3. **类型检查**：运行`yarn type-check`
4. **代码规范**：运行`yarn lint`
5. **格式检查**：运行`yarn prettier --check`
6. **架构一致性**：检查是否符合项目架构模式和设计系统
7. **知识归档**：如果是新的问题模式，必须更新架构文档或添加到已知问题列表

## 🚨 常见问题预防

### 水合错误预防模式
```typescript
// ❌ 错误：直接使用浏览器API
const isMobile = window.innerWidth < 768;

//  正确：使用防御性Hook
const useIsMobile = () => {
  const isClient = useIsClient();
  const [isMobile, setIsMobile] = useState(false);
  
  useEffect(() => {
    if (isClient) {
      setIsMobile(window.innerWidth < 768);
    }
  }, [isClient]);
  
  return isMobile;
};
```

### 类型安全预防模式
```typescript
// ❌ 错误：使用any类型
const handleData = (data: any) => { ... };

//  正确：定义完整interface
interface ApiResponse<T> {
  data: T;
  error?: string;
  success: boolean;
}

const handleData = <T>(response: ApiResponse<T>) => { ... };
```

### 可选依赖处理模式
```typescript
// ❌ 错误：忽略可选依赖类型
const { Preferences } = await import('@capacitor/preferences');

//  正确：创建类型声明文件 + 安全导入
// 1. 创建 src/types/capacitor.d.ts
// 2. 使用try-catch处理导入失败
try {
  const { Preferences } = await import('@capacitor/preferences');
  // 使用Preferences
} catch {
  // fallback逻辑
}
```

### 语言切换预防模式
```typescript
// ❌ 错误：queryKey固定，语言切换时不更新
const useFrontendCategories = () => {
  return useQuery({
    queryKey: ['frontendCategories'], // 缺少locale参数
    queryFn: () => frontendBlogApi.getCategories(),
  });
};

//  正确：queryKey包含locale，语言切换时重新获取
const useFrontendCategories = () => {
  const params = useParams();
  const locale = (params.locale as string) || 'zh';
  
  return useQuery({
    queryKey: ['frontendCategories', locale], // 包含locale参数
    queryFn: () => frontendBlogApi.getCategories(),
  });
};

//  正确：HTTP客户端自动添加lang参数
// 在HTTP拦截器中自动添加lang查询参数
instance.interceptors.request.use((config) => {
  const lang = localStorage.getItem('locale') || 'zh';
  config.params = { ...config.params, lang };
  config.headers['Accept-Language'] = lang;
  return config;
});
```

### 问题修复预防模式
```typescript
// ❌ 错误：消防员思维
// 只修复出错的那一行，错误消失就完事了
const locale = cookieLocale || DEFAULT_LOCALE;

//  正确：架构师思维
// 1. 理解根因 2. 修复问题 3. 预防未来 4. 记录知识
const locale = cookieLocale && LOCALES.includes(cookieLocale) ? cookieLocale : DEFAULT_LOCALE;

// 修复完成后必须问自己：
// ▢ 还有哪里有同样的问题？
// ▢ 为什么这个函数被删除了还有地方在引用？
// ▢ 我应该把这个发现记录在哪里？
```

## 📚 参考文档

1. **AI协作模板**：`ai-task-template.md` - 使用结构化模板分配任务
2. **技术文档标准**：`docs/TECHNICAL_DOCUMENT_TEMPLATE.md` - 七层黄金文档结构
3. **项目架构**：`ARCHITECTURE_CN.md` - 整体架构设计
4. **当前任务**：`.github/copilot-instructions.md` - 每次对话开始前必须检查

## 📝 技术架构文档创作规范

### 1. 文档定位三要素
- **文档类型**：必须明确是架构设计/实施手册/技术参考
- **目标读者**：必须明确面向决策者/架构师/开发者
- **详细程度**：必须明确简洁概要/详细完整/代码示例

### 2. 层次结构强制
所有技术架构文档必须包含以下层次：
1. **目标层**：解决什么问题，达到什么目标
2. **架构层**：高层次设计，核心组件关系
3. **指引层**：实施方向，不深入具体代码
4. **验证层**：如何衡量成功，可量化指标

### 3. 视觉表达优先
关键信息必须视觉化表达：
-  **架构图**：使用ASCII或图表展示组件关系
-  **对比表格**：方案对比、平台差异、决策分析
-  **决策树**：条件判断逻辑
-  **文件结构树**：代码组织方案

### 4. 信息密度控制
- 每章节不超过10个要点
- 表格不超过5列
- 代码示例不超过5行（如必须）
- 保持整体简洁，聚焦关键决策点

### 5. 完整性检查清单
文档完成后必须验证：
- [ ] 是否有明确文档定位？
- [ ] 是否有清晰的架构图？
- [ ] 是否有实施指引？
- [ ] 是否有验证指标？
- [ ] 是否符合简洁性要求？

### 6. 创作流程规范
1. **需求分析**（5分钟）：确认文档类型、读者、详细程度
2. **架构设计**（10分钟）：创建架构图，定义核心组件
3. **内容填充**（15分钟）：按层次结构组织内容
4. **质量检查**（5分钟）：应用完整性检查清单

### 7. 错误预防模式
```markdown
# ❌ 错误：工程师思维文档
- 直接从技术方案开始
- 详细代码示例堆砌
- 缺乏价值阐述
- 没有决策支持

#  正确：架构思维文档
## 📋 文档目标
**简洁架构文档**：只包含核心架构设计

## 🎯 核心目标
- 解决什么问题
- 达到什么效果

## 🏛️ 架构设计
[架构图]
- 核心组件
- 数据流

## 📁 实施指引
[文件结构]
- 关键步骤
- 决策点

## 📊 成功验证
[指标表格]
- 可衡量目标
- 验证方法
```

## ⚡ 立即执行检查清单

在生成任何代码前，AI必须：
- [ ] 阅读此宪法文件
- [ ] 检查`.github/copilot-instructions.md`中的当前任务
- [ ] 执行"思考过程要求"中的5项分析
- [ ] 确保不违反任何"绝对禁令"

> **记住**：你不是在写几行代码，你是在维护一个生产级全栈应用。每个决策都有系统级影响。