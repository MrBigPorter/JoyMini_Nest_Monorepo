# Lucky Nest Monorepo - AI助手引导规则

> **核心指引**：每次对话开始前，必须先检查 `.github/copilot-instructions.md` 中的当前任务和完整规范。

> **问题解决原则**：绝对不能乱猜！在解决问题时，必须基于代码分析和实际证据，而不是猜测。如果信息不足，应该先使用工具（如read_file、search_files）收集足够信息，再进行推理和修复。验证假设时，优先通过阅读代码和运行检查来确认，而不是基于未经证实的推断。

> **AI协作优化**：为提高AI代码质量，建议使用 `ai-task-template.md` 中的模板分配任务。模板包含上下文背景、核心要求、检查清单，能显著减少返工和重复沟通。
