# 🤖 AI协作任务模板 v1.0

> **核心原则**：好的任务描述 = 50%的成功率。使用此模板可以显著提升AI代码质量，减少返工。

---

## 🎯 任务类型选择

请根据任务类型选择对应的模板：

### 📋 模板A：功能开发

### 🔧 模板B：问题修复

### 🏗️ 模板C：代码重构

### 📚 模板D：文档编写

---

## 📋 模板A：功能开发

### 1. 上下文背景

- **相关文件**：`src/components/X.tsx`, `src/hooks/useY.ts`
- **技术栈**：Next.js 15, React Query, Tailwind CSS, 国际化路由
- **特殊约束**：国际化路由 `/[locale]/path`，共享QueryClient，遵循DRY原则

### 2. 功能需求

- **用户故事**：作为[用户角色]，我想要[做什么]，以便[达到什么目的]
- **验收标准**：
  - [ ] 功能A正常工作
  - [ ] 功能B边界情况处理
  - [ ] 响应式适配完成
  - [ ] 多语言支持

### 3. 核心要求（请AI重点关注）

1. **DRY检查**：检查是否有重复逻辑可以抽取公共函数
2. **一致性检查**：确保相关组件逻辑统一
3. **边界情况**：处理语言前缀、空状态、错误回退
4. **时序预判**：异步操作考虑竞态条件

### 4. 技术约束

- 必须使用现有的 `@repo/ui` 组件库
- 必须遵循项目代码风格和架构模式
- 必须兼容国际化路由格式
- 必须考虑移动端适配

---

## 🔧 模板B：问题修复

### 1. 问题现象

- **具体表现**：[描述用户看到的问题现象]
- **复现步骤**：
  1. 访问 `/zh/categories`
  2. 点击某个分类
  3. 观察到[问题现象]

### 2. 预期行为

- **正常情况**：[描述应该看到什么]
- **当前情况**：[描述实际看到什么]

### 3. 已排查信息

- **相关文件**：`src/components/X.tsx` (第42行)
- **错误信息**：`Error: Cannot read property 'xxx' of undefined`
- **环境信息**：Chrome 浏览器，中文语言环境

### 4. 修复要求

1. **根因分析**：请先分析问题根本原因，不要直接贴代码
2. **影响范围**：评估这个修复会影响哪些其他功能
3. **测试方案**：如何验证修复成功
4. **预防措施**：如何避免类似问题再次发生

---

## 🏗️ 模板C：代码重构

### 1. 重构目标

- **当前问题**：代码重复、维护困难、性能问题、类型不安全
- **重构范围**：`src/components/` 目录下的相关文件
- **不变量**：必须保持现有功能完全不变

### 2. 重构原则

1. **DRY优先**：抽取重复逻辑为公共函数
2. **单一职责**：每个函数/组件只做一件事
3. **类型安全**：消除any类型，完善TypeScript定义
4. **性能优化**：减少不必要的重渲染

### 3. 具体任务

- [ ] 分析现有代码的重复模式
- [ ] 设计公共函数/组件接口
- [ ] 逐步替换，保持功能不变
- [ ] 添加单元测试

### 4. 验收标准

- [ ] 代码行数减少 X%
- [ ] 类型覆盖率提升到 100%
- [ ] 编译通过，测试通过
- [ ] 性能指标无下降

---

## 📚 模板D：文档编写

### 1. 文档目的

- **目标读者**：[开发者/产品经理/测试人员]
- **使用场景**：[开发参考/部署指南/故障排查]
- **预期效果**：读者能够[完成什么任务]

### 2. 内容结构

1. **问题描述**：我们遇到了什么问题
2. **根因分析**：根本原因是什么
3. **方案选型**：有哪些方案，为什么选择这个
4. **系统架构**：整体设计是什么样的
5. **工作流程**：数据是怎么流动的
6. **实现细节**：关键实现点和边界条件
7. **成本性能**：生产环境运行指标

### 3. 写作要求

- 使用中文，技术术语保持英文
- 包含代码示例和图表
- 提供实际可执行的命令
- 注明相关文件和版本信息

---

## 🚀 通用检查清单（每次任务都适用）

### ✅ 架构思维检查清单

1. **重复检查**：这个逻辑在其他地方存在吗？ → 建议抽取公共函数
2. **系统思考**：这个改动会影响哪些相关部分？ → 列出并确保同步
3. **时序预判**：如果有两个请求同时到达会发生什么？ → 添加防护机制
4. **模式匹配**：是否符合项目现有模式？ → 保持一致风格

### ✅ 代码质量检查清单

1. **DRY原则**：无重复代码（超过3行相似就应抽取）
2. **单一职责**：函数只做一件事，组件职责清晰
3. **可测试性**：逻辑清晰可测，便于添加单元测试
4. **可维护性**：有注释、有类型、有文档

### ✅ 用户体验检查清单

1. **加载状态**：异步操作有加载提示吗？
2. **错误状态**：出错时有友好提示吗？
3. **空状态**：没有数据时显示什么？
4. **交互反馈**：用户操作有即时反馈吗？

---

## 💡 三条"金规玉律"（必问）

1. **"重复检查"法则**

   > 每当写一个函数时，问自己："这个逻辑在其他地方存在吗？"

2. **"系统思考"法则**

   > 每当改一个组件时，问自己："这个改动会影响哪些相关部分？"

3. **"时序预判"法则**
   > 每当处理异步时，问自己："如果有两个请求同时到达会发生什么？"

---

## 📁 文件位置建议

### 公共工具函数

```
src/lib/utils/          # 通用工具函数
src/lib/hooks/          # 自定义React Hooks
src/lib/types/          # TypeScript类型定义
```

### 组件分类

```
src/components/         # 通用组件
src/components/blog/    # 博客相关组件
src/components/shared/  # 共享组件
```

### 文档位置

```
docs/                   # 项目文档
docs/blog/              # 博客系统文档
docs/blog/features/     # 功能特性文档
```

---

## 🎯 使用示例

### 示例1：导航栏修复任务

```markdown
## 🎯 任务类型：问题修复

### 上下文背景

- **相关文件**：`src/components/Header.tsx`, `src/components/BottomNavigation.tsx`
- **技术栈**：Next.js 15, 国际化路由 `/zh/xxx`
- **特殊约束**：多语言路由前缀，PC端和移动端需要一致

### 问题现象

- 导航栏只有首页能正确显示选中状态，其他tab（分类、标签、收藏、关于）都无法高亮
- 路径格式：`pathname = '/zh/categories'`, `item.href = '/categories'`

### 修复要求

1. 分析为什么只有首页能匹配成功
2. 设计智能路径匹配函数，处理语言前缀
3. 确保PC端和移动端使用相同的逻辑
4. 考虑抽取公共函数避免重复代码
```

### 示例2：评论系统优化

```markdown
## 🎯 任务类型：功能开发

### 上下文背景

- **相关文件**：`src/lib/hooks/useComments.ts`, `src/lib/hooks/useCommentsInfiniteQuery.ts`
- **技术栈**：React Query, 乐观更新, 状态轮询
- **特殊约束**：共享QueryClient，处理竞态条件

### 功能需求

- 用户提交评论后立即显示（乐观更新）
- 评论需要后台审核，审核通过后更新状态
- 支持无限滚动和普通列表两种展示方式

### 核心要求

1. **缓存同步**：确保两个Hook使用相同的QueryClient，数据一致
2. **竞态防护**：处理乐观更新和服务器响应的时序问题
3. **重复过滤**：防止临时ID和真实ID冲突
4. **错误回退**：网络失败时的降级处理
```

---

## 📈 效果评估

### 使用前

- ❌ AI只关注局部问题，忽略系统一致性
- ❌ 重复代码多，维护成本高
- ❌ 竞态条件频发，调试困难
- ❌ 沟通成本高，多次往返

### 使用后

- ✅ AI从系统角度思考问题
- ✅ 代码复用率高，DRY原则贯彻
- ✅ 时序问题预先防护
- ✅ 一次沟通，高效产出

---

## 🔄 持续改进

### 反馈收集

每次使用模板后，记录：

1. **哪些有效**：AI正确理解了哪些要求
2. **哪些不足**：AI忽略了哪些重要点
3. **改进建议**：模板可以如何优化

### 版本迭代

- **v1.0** (2026-04-17)：基于导航栏修复经验创建
- **v1.1** (计划)：增加更多实际案例
- **v1.2** (计划)：添加项目特定约束模板

---

> **最后提醒**：好的任务描述是成功的一半。花2分钟填写模板，可以节省20分钟的返工时间。

**立即使用**：复制对应模板 → 填写具体内容 → 发送给AI
